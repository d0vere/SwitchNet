# Http Api

This file belongs to the SwitchNet project.

SwitchNet uses English as its exclusive canonical language. The v1.9.0 release
preserves the controller behavior, networking architecture, firmware features,
OTA support, web configuration, and rumble architecture validated before the
language migration.

For implementation details, refer to the current source code and runtime
diagnostics.
